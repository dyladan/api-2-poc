{
  "name": "api-2",
  "version": "1.0.0",
  "main": "build/src/index.js",
  "files": [
    "build/**/*.js",
    "build/**/*.js.map",
    "build/**/*.d.ts",
    "build/**/*.d.ts.map"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build",
    "prebuild": "npm run clean",
    "prepack": "npm run build",
    "check": "tsc --noEmit",
    "start": "ts-node src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}